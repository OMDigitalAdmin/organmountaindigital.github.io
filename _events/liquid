<ul>
  {% for event in site.events %}
    <li>
      <a href="{{ event.url | relative_url }}">{{ event.title }}</a><br>
      <small>{{ event.date | date: "%B %d, %Y" }}</small>
    </li>
  {% endfor %}
</ul>
